name: Reddit

options:
  bundleIdPrefix: net.rafaeltoledo.reddit.ios

targets:
  RedditApp:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources: Reddit
    dependencies:
      - framework: ../shared/build/fat-framework/release/Shared.framework
